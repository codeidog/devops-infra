name: 'Terraform Deploy'

on:
  push:
    branches:
      - main
    paths:
      - '**/*.tf'
      - '**/*.tfvars'
      - '.github/workflows/*.yml'

env:
  TF_VERSION: '1.13.5'
  AWS_REGION: 'eu-west-1'

jobs:
  get-pr-info:
    name: 'Get PR Number and Run ID'
    runs-on: ubuntu-latest
    outputs:
      pr-number: ${{ steps.set-outputs.outputs.pr-number }}
      run-id: ${{ steps.set-outputs.outputs.run-id }}

    permissions:
      pull-requests: read
      actions: read
      contents: read
    steps:
    - name: Get PR Number & Workflow Run ID
      id: pr-info
      uses: actions/github-script@v7
      with:
        github-token: ${{ secrets.GITHUB_TOKEN }}
        script: |
          // Get the commit SHA from the push event
          const commitSha = context.sha;
          console.log(`Looking for PR associated with commit: ${commitSha}`);
          
          // Search for PRs that were merged with this commit
          const { data: pullRequests } = await github.rest.repos.listPullRequestsAssociatedWithCommit({
            owner: context.repo.owner,
            repo: context.repo.repo,
            commit_sha: commitSha,
          });
          
          console.log(`Found ${pullRequests.length} PRs associated with commit`);
          
          // Find the merged PR
          const mergedPR = pullRequests.find(pr => pr.state === 'closed' && pr.merged_at);
          
          if (!mergedPR) {
            console.log('No merged PR found, this might be a direct push to main');
            return { prNumber: '', runId: '' };
          }
          
          const prNumber = mergedPR.number.toString();
          console.log(`Found merged PR: ${prNumber}`);
          console.log(`PR Merge Commit SHA: ${mergedPR.merge_commit_sha}`);
          console.log(`PR Head SHA: ${mergedPR.head.sha}`);
          
          // Now get the workflow run ID for this PR
          console.log(`Looking for workflow run for PR #${prNumber}`);
          
          // Get workflow runs for the terraform-pr workflow
          // Note: Don't filter by head_sha since squash merges create new commit SHAs
          const { data: workflowRuns } = await github.rest.actions.listWorkflowRuns({
            owner: context.repo.owner,
            repo: context.repo.repo,
            workflow_id: 'terraform-pr.yml',
            event: 'pull_request',
            status: 'success',
            head_sha: mergedPR.head.sha,
            per_page: 50
          });
          
          console.log(`Found ${workflowRuns.workflow_runs.length} successful workflow runs for commit ${commitSha}`);
          
          // Find the PR run
          const targetRun = workflowRuns.workflow_runs.find(run => 
            run.event === 'pull_request' && run.conclusion === 'success'
          );
          
          if (targetRun) {
            console.log(`Found workflow run ${targetRun.id} for PR #${prNumber}`);
            return { prNumber, runId: targetRun.id.toString() };
          }
          
          
          console.log(`No successful workflow run found for PR #${prNumber}`);
          return { prNumber, runId: '' };

    - name: Set Job Outputs
      id: set-outputs
      run: |
        PR_INFO='${{ steps.pr-info.outputs.result }}'
        echo "Raw output: $PR_INFO"
        
        # Parse JSON output
        PR_NUMBER=$(echo "$PR_INFO" | jq -r '.prNumber // ""')
        RUN_ID=$(echo "$PR_INFO" | jq -r '.runId // ""')
        
        echo "pr-number=$PR_NUMBER" >> $GITHUB_OUTPUT
        echo "run-id=$RUN_ID" >> $GITHUB_OUTPUT
        
        echo "Parsed PR Number: $PR_NUMBER"
        echo "Parsed Run ID: $RUN_ID"

  terraform-apply:
    name: 'Terraform Apply'
    runs-on: ubuntu-latest
    needs: get-pr-info
    if: needs.get-pr-info.outputs.pr-number != '' && needs.get-pr-info.outputs.run-id != ''
    defaults:
      run:
        working-directory: ./

    permissions:
      deployments: write
      statuses: write
      contents: read
      id-token: write

    steps:
    - name: Checkout
      uses: actions/checkout@v4

    - name: Configure AWS credentials
      uses: aws-actions/configure-aws-credentials@v4
      with:
        role-to-assume: ${{ secrets.AWS_ROLE_ARN }}
        aws-region: ${{ env.AWS_REGION }}

    - name: Setup Terraform
      uses: hashicorp/setup-terraform@v3
      with:
        terraform_version: ${{ env.TF_VERSION }}

    - name: Download Plan Artifact
      uses: actions/download-artifact@v5
      id: download-artifact
      with:
        name: terraform-plan-${{ needs.get-pr-info.outputs.pr-number }}
        run-id: ${{ needs.get-pr-info.outputs.run-id }}
        path: ./
    
    - name: Verify Plan File
      id: verify
      run: |
        if [ -f "tfplan-${{ needs.get-pr-number.outputs.pr-number }}" ]; then
          echo "Plan file found"
        else
          echo "Plan file not found"
          exit 1
        fi

    - name: Terraform Init
      id: init
      run: terraform init

    - name: Terraform Apply with Artifact
      id: apply-artifact
      if: steps.verify.outputs.plan-exists == 'true'
      run: |
        echo "Applying plan from artifact"
        terraform apply -auto-approve tfplan-${{ needs.get-pr-number.outputs.pr-number }}

    - name: Generate Deployment Summary
      id: summary
      if: steps.apply-artifact.outcome == 'success'
      run: |
        echo "## Deployment Summary" > deployment_summary.md
        echo "✅ **Deployment Status**: SUCCESS" >> deployment_summary.md
        echo "🕐 **Deployment Time**: $(date)" >> deployment_summary.md
        echo "🔗 **Commit**: ${{ github.sha }}" >> deployment_summary.md
        echo "👤 **Deployed by**: ${{ github.actor }}" >> deployment_summary.md
        echo "🎯 **PR Number**: #${{ needs.get-pr-number.outputs.pr-number }}" >> deployment_summary.md
        echo "📋 **Plan Source**: ${{ steps.verify.outputs.plan-exists == 'true' && 'Artifact' || 'Fresh' }}" >> deployment_summary.md
        echo "" >> deployment_summary.md
        echo "### Resources Applied:" >> deployment_summary.md
        
        # Get current state resources
        terraform show -json | jq -r '.values.root_module.resources[]?.address' >> deployment_summary.md 2>/dev/null || echo "Unable to list resources" >> deployment_summary.md

    - name: Comment Deployment Success
      uses: actions/github-script@v7
      if: steps.apply-artifact.outcome == 'success'
      with:
        github-token: ${{ secrets.GITHUB_TOKEN }}
        script: |
          const fs = require('fs');
          
          let summary = '🚀 **Deployment Successful!**\n\n';
          summary += `✅ Terraform apply completed successfully\n`;
          summary += `🕐 Deployed at: ${new Date().toISOString()}\n`;
          summary += `🔗 Commit: ${context.sha.substring(0, 7)}\n`;
          summary += `👤 Deployed by: ${context.actor}\n`;
          summary += `🎯 PR: #${{ needs.get-pr-number.outputs.pr-number }}\n`;
          summary += `📋 Plan source: ${{ steps.verify.outputs.plan-exists == 'true' && 'PR Artifact' || 'Fresh Plan' }}\n\n`;
          
          // Add plan output if available
          try {
            const planOutput = fs.readFileSync('./plan_output.txt', 'utf8');
            if (planOutput && planOutput.length < 50000) {  // Limit size for comment
              summary += `<details><summary>📋 Deployment Details</summary>\n\n\`\`\`terraform\n${planOutput}\n\`\`\`\n\n</details>`;
            }
          } catch (e) {
            console.log('Plan output file not found or too large');
          }

          // Add comment to the commit
          await github.rest.repos.createCommitComment({
            owner: context.repo.owner,
            repo: context.repo.repo,
            commit_sha: context.sha,
            body: summary
          });

    - name: Handle Deployment Failure
      uses: actions/github-script@v7
      if: steps.apply-artifact.outcome == 'failure'
      with:
        github-token: ${{ secrets.GITHUB_TOKEN }}
        script: |
          const summary = `❌ **Deployment Failed!**

          🚨 Terraform apply failed for commit ${context.sha.substring(0, 7)}
          👤 Attempted by: ${context.actor}
          🕐 Failed at: ${new Date().toISOString()}
          🎯 PR: #${{ needs.get-pr-number.outputs.pr-number }}
          📋 Plan source: ${{ steps.verify.outputs.plan-exists == 'true' && 'PR Artifact' || 'Fresh Plan' }}

          Please check the [workflow logs](https://github.com/${context.repo.owner}/${context.repo.repo}/actions/runs/${context.runId}) for details and fix the issues before retrying.`;

          await github.rest.repos.createCommitComment({
            owner: context.repo.owner,
            repo: context.repo.repo,
            commit_sha: context.sha,
            body: summary
          });

    - name: Upload Deployment Logs
      uses: actions/upload-artifact@v4
      if: always()
      with:
        name: terraform-deployment-logs-${{ github.run_number }}
        path: |
          plan_output.txt
          deployment_summary.md
        retention-days: 90

    - name: Set Deployment Status - Success
      if: steps.apply-artifact.outcome == 'success'
      uses: actions/github-script@v7
      with:
        script: |

          await github.rest.repos.createDeploymentStatus({
            owner: context.repo.owner,
            repo: context.repo.repo,
            deployment_id: '${{ steps.deployment.outputs.result }}',
            state: 'success',
            description: `✅ Terraform deployment successful`,
            environment_url: 'https://console.aws.amazon.com/eks/home?region=${{ env.AWS_REGION }}#/clusters'
          });

    - name: Set Deployment Status - Failure
      if: steps.apply-artifact.outcome == 'failure'
      uses: actions/github-script@v7
      with:
        github-token: ${{ secrets.GITHUB_TOKEN }}
        script: |
          await github.rest.repos.createDeploymentStatus({
            owner: context.repo.owner,
            repo: context.repo.repo,
            deployment_id: '${{ steps.deployment.outputs.result }}',
            state: 'failure',
            description: '❌ Terraform deployment failed',
            log_url: `https://github.com/${context.repo.owner}/${context.repo.repo}/actions/runs/${context.runId}`
          });
